20161109
L12


2.
If supervisor is a manager of his employees:

SELECT E.FNAME FROM EMPLOYEE AS E WHERE SSN IN (SELECT SUPER_SSN FROM EMPLOYEE WHERE SALARY > (SELECT AVG(SALARY) FROM EMPLOYEE) GROUP BY SUPER_SSN HAVING COUNT(SUPER_SSN)>1);
+-------+
| FNAME |
+-------+
| James |
+-------+



3.
SELECT COUNT(SEX), SUPER_SSN FROM EMPLOYEE AS E WHERE SEX='F' GROUP BY SUPER_SSN;
SELECT COUNT(SEX), SUPER_SSN FROM EMPLOYEE AS E WHERE SEX='M' GROUP BY SUPER_SSN;
